<%= form_with(model: colab, local: true) do |form| %>
  <% if colab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(colab.errors.count, "error") %> prohibited this colab from being saved:</h2>

      <ul>
      <% colab.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :colab_nome %>
  </div>

  <div class="field">
    <%= form.label :list_id %>
    <%= form.text_field :list_id, id: :colab_list_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
